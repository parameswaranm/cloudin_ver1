import React, { Fragment } from 'react';

import Slider from './plugins/Slider';

const Container = () => {
  return (
    <Fragment>
      <Slider />
    </Fragment>
  );
};

export default Container;
